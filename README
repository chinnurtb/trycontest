==简介==
RTB的一个真实的实验。
==开发==
我们将程序所有的文件都放到目录conf data下面。 在主程序中读取相关文件的时候只需要提供文件名即可。
参考
    src/java/com/ipingyou/contest/algorithm/DemoBid.java::readModelFileToLines
这里有一个假设conf目录下的文件名必须于data下的文件名不一样

===编译===
ant clean
ant MainDriver
ant junit

如果用eclipse开发，将该项目import为一个general的项目，然后在项目的主目录下添加.project .classpath两个文件，文件的内容
分别参考eclipse/project eclipse/classpath
===运行===
java -cp bin/bid-1.0.0.jar  com.ipinyou.contest.algorithm.DemoBid


